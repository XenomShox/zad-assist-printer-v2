import ChatHeroTitle from "@/components/dashboard/chat/chat-hero-title";

const Dashboard = () => {
  return (
    <section className="flex h-[calc(100vh-4rem)] flex-1 flex-col justify-center gap-4">
      <ChatHeroTitle />
    </section>
  );
};

export default Dashboard;
